# 🏗️ Arquitetura de Assinaturas - AppCheckin

## Diagrama de Componentes

```
┌──────────────────────────────────────────────────────────────────────────┐
│                          CLIENTE (Browser/Mobile)                         │
├──────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  ┌─ AssinaturasScreen (React Native) ────────────────────────────────┐  │
│  │ • Listagem de assinaturas com filtros                             │  │
│  │ • Busca por aluno/plano                                           │  │
│  │ • Seleção de academia (SuperAdmin)                                │  │
│  │ • Modal com detalhes e ações                                      │  │
│  │ • Estados: ativa, suspensa, cancelada, vencida                    │  │
│  └────────────────────┬────────────────────────────────────────────────┘  │
│                       │                                                    │
│  ┌────────────────────▼─────────────────────────────────────────────┐   │
│  │ assinaturaService.js                                             │   │
│  │ • listar() / listarTodas()                                        │   │
│  │ • criar() / atualizar() / buscar()                                │   │
│  │ • renovar() / suspender() / reativar() / cancelar()               │   │
│  │ • listarProximasVencer() / relatorio()                            │   │
│  └────────────────────┬─────────────────────────────────────────────┘   │
│                       │                                                    │
│  ┌────────────────────▼─────────────────────────────────────────────┐   │
│  │ API Client (axios/fetch)                                          │   │
│  │ Headers: Authorization: Bearer {JWT}                              │   │
│  │ Content-Type: application/json                                    │   │
│  └────────────────────┬─────────────────────────────────────────────┘   │
│                       │                                                    │
└───────────────────────┼────────────────────────────────────────────────────┘
                        │ HTTP Request
                        ▼
┌──────────────────────────────────────────────────────────────────────────┐
│                   BACKEND API (PHP/Slim Framework)                        │
├──────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  ┌─ Middleware Stack ────────────────────────────────────────────────┐  │
│  │ 1. AuthMiddleware (validação JWT)                                 │  │
│  │ 2. TenantMiddleware (isolamento por academia)                     │  │
│  │ 3. AdminMiddleware ou SuperAdminMiddleware                        │  │
│  └──────────────────┬─────────────────────────────────────────────────┘  │
│                     │                                                     │
│  ┌──────────────────▼──────────────────────────────────────────────┐    │
│  │ Router (Slim)                                                    │    │
│  │ GET    /admin/assinaturas                                        │    │
│  │ GET    /admin/assinaturas/{id}                                   │    │
│  │ POST   /admin/assinaturas                                        │    │
│  │ PUT    /admin/assinaturas/{id}                                   │    │
│  │ POST   /admin/assinaturas/{id}/renovar                           │    │
│  │ POST   /admin/assinaturas/{id}/suspender                         │    │
│  │ POST   /admin/assinaturas/{id}/reativar                          │    │
│  │ POST   /admin/assinaturas/{id}/cancelar                          │    │
│  │ GET    /superadmin/assinaturas                                   │    │
│  └──────────────────┬──────────────────────────────────────────────┘    │
│                     │                                                     │
│  ┌──────────────────▼──────────────────────────────────────────────┐    │
│  │ AssinaturaController.php                                         │    │
│  │ • listar() - Lista com paginação e filtros                       │    │
│  │ • listarTodas() - Para SuperAdmin                                │    │
│  │ • buscar() - Detalhes com histórico                              │    │
│  │ • criar() - Validação e inserção                                 │    │
│  │ • suspender() - Atualiza status                                  │    │
│  │ • cancelar() - Marca como cancelada                              │    │
│  │ • renovar() - Estende vencimento e registra histórico            │    │
│  │ • reativar() - De suspensa para ativa                            │    │
│  │ • calcularDataVencimento() - Lógica de ciclos                    │    │
│  └──────────────────┬──────────────────────────────────────────────┘    │
│                     │                                                     │
│  ┌──────────────────▼──────────────────────────────────────────────┐    │
│  │ PDO / Prepared Statements                                        │    │
│  │ • Validação de entrada                                           │    │
│  │ • Queries parametrizadas                                         │    │
│  │ • Transações (quando necessário)                                 │    │
│  └──────────────────┬──────────────────────────────────────────────┘    │
│                     │                                                     │
└─────────────────────┼──────────────────────────────────────────────────────┘
                      │ SQL Query
                      ▼
┌──────────────────────────────────────────────────────────────────────────┐
│                      DATABASE (MySQL 5.7+)                               │
├──────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  ┌─ assinaturas ────────────────────────────────────────────────────┐   │
│  │ PK: id                                                            │   │
│  │ FK: aluno_id (references alunos)                                 │   │
│  │ FK: plano_id (references planos)                                 │   │
│  │ FK: academia_id (references academias)                           │   │
│  │ Campos: status, datas, valores, forma_pagamento, etc            │   │
│  │ Índices: status, data_vencimento, aluno_id, academia_id         │   │
│  └───────────────────────────────────────────────────────────────────┘  │
│                                                                            │
│  ┌─ assinatura_renovacoes ───────────────────────────────────────────┐  │
│  │ PK: id                                                             │  │
│  │ FK: assinatura_id (references assinaturas)                        │  │
│  │ Campos: data_renovacao, valor, forma_pagamento, etc              │  │
│  │ Índice: assinatura_id                                             │  │
│  └────────────────────────────────────────────────────────────────────┘  │
│                                                                            │
│  ┌─ Relationships ───────────────────────────────────────────────────┐  │
│  │ assinaturas.aluno_id ──→ alunos.id                                │  │
│  │ assinaturas.plano_id ──→ planos.id                                │  │
│  │ assinaturas.academia_id ──→ academias.id                          │  │
│  │ assinatura_renovacoes.assinatura_id ──→ assinaturas.id            │  │
│  └────────────────────────────────────────────────────────────────────┘  │
│                                                                            │
└──────────────────────────────────────────────────────────────────────────┘
```

## Fluxo de Dados - Criar Assinatura

```
┌─────────────────────┐
│   Usuário Admin      │
│  preenche formulário │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────────────────────┐
│ AssinaturasScreen.criar()           │
│ Valida dados locais                 │
└──────────┬──────────────────────────┘
           │
           ▼
┌─────────────────────────────────────┐
│ assinaturaService.criar(dados)      │
│ Prepara payload                     │
│ POST /admin/assinaturas             │
└──────────┬──────────────────────────┘
           │
           ▼
┌─────────────────────────────────────┐
│ AuthMiddleware                      │
│ Valida JWT token                    │
└──────────┬──────────────────────────┘
           │
           ▼
┌─────────────────────────────────────┐
│ TenantMiddleware                    │
│ Extrai academia_id do token         │
└──────────┬──────────────────────────┘
           │
           ▼
┌─────────────────────────────────────┐
│ AdminMiddleware                     │
│ Verifica se é Admin/SuperAdmin      │
└──────────┬──────────────────────────┘
           │
           ▼
┌─────────────────────────────────────┐
│ AssinaturaController.criar()        │
│ 1. Valida campos obrigatórios       │
│ 2. Verifica se aluno existe         │
│ 3. Busca dados do plano             │
│ 4. Verifica assinatura duplicada    │
│ 5. Calcula data de vencimento       │
│ 6. Insere no banco                  │
└──────────┬──────────────────────────┘
           │
           ▼
┌─────────────────────────────────────┐
│ INSERT INTO assinaturas             │
│ VALUES (aluno, plano, academia...)  │
└──────────┬──────────────────────────┘
           │
           ▼
┌─────────────────────────────────────┐
│ 201 Created (JSON Response)         │
│ id, status, datas, valores          │
└──────────┬──────────────────────────┘
           │
           ▼
┌─────────────────────────────────────┐
│ Frontend recebe resposta            │
│ Mostra toast de sucesso             │
│ Atualiza lista                      │
└─────────────────────────────────────┘
```

## Fluxo de Dados - Renovar Assinatura

```
┌──────────────────┐
│ Usuário Admin    │
│ Clica em Renovar │
└────────┬─────────┘
         │
         ▼
┌────────────────────────────────────┐
│ handleRenovar(assinatura)          │
│ Abre ConfirmModal                  │
└────────┬───────────────────────────┘
         │
         ▼
┌────────────────────────────────────┐
│ Usuário confirma ação              │
└────────┬───────────────────────────┘
         │
         ▼
┌────────────────────────────────────┐
│ assinaturaService.renovar(id)      │
│ POST /admin/assinaturas/:id/renovar│
└────────┬───────────────────────────┘
         │
         ▼
┌────────────────────────────────────┐
│ AssinaturaController.renovar()     │
│ 1. Valida assinatura existe        │
│ 2. Valida status é 'ativa'         │
│ 3. Calcula nova data vencimento    │
│ 4. UPDATE assinaturas              │
│ 5. INSERT assinatura_renovacoes    │
└────────┬───────────────────────────┘
         │
         ▼
┌────────────────────────────────────┐
│ 200 OK (JSON Response)             │
│ Nova data vencimento               │
│ Histórico atualizado               │
└────────┬───────────────────────────┘
         │
         ▼
┌────────────────────────────────────┐
│ Frontend                           │
│ Toast: "Renovada com sucesso"      │
│ Atualiza lista                     │
└────────────────────────────────────┘
```

## Estados e Transições

```
┌─────────────────────────────────────────────────────────────┐
│                    Estado da Assinatura                      │
├─────────────────────────────────────────────────────────────┤

                            ┌─ ATIVA ─┐
                            │         │
                            └────┬────┘
                                 │
                    ┌────────────┴──────────────┐
                    │                           │
                    ▼                           ▼
            ┌──────────────┐            ┌──────────────┐
            │  SUSPENSA    │            │  CANCELADA   │
            │              │            │              │
            │ (reativável) │            │ (permanente) │
            └──────┬───────┘            └──────────────┘
                   │
                   ▼
            ┌──────────────┐
            │  ATIVA       │
            │ (reativada)  │
            └──────────────┘

┌─────────────────────────────────────────────────────────────┐
│                      Ações por Estado                        │
├─────────────────────────────────────────────────────────────┤

ATIVA:
  ├─ renovar() → ATIVA (com data vencimento estendida)
  ├─ suspender() → SUSPENSA
  └─ cancelar() → CANCELADA

SUSPENSA:
  ├─ reativar() → ATIVA
  └─ cancelar() → CANCELADA

CANCELADA:
  └─ (sem ações possíveis)

VENCIDA:
  ├─ renovar() → ATIVA
  └─ cancelar() → CANCELADA
```

## Estrutura de Resposta - Exemplo

### Sucesso - Criar Assinatura
```json
{
  "type": "success",
  "message": "Assinatura criada com sucesso",
  "data": {
    "assinatura": {
      "id": 25,
      "aluno_id": 5,
      "plano_id": 2,
      "academia_id": 1,
      "status": "ativa",
      "data_inicio": "2025-01-15",
      "data_vencimento": "2025-02-15",
      "valor_mensal": 150.00,
      "forma_pagamento": "cartao_credito",
      "ciclo_tipo": "mensal",
      "renovacoes_restantes": 12,
      "criado_em": "2025-01-15T10:30:00Z",
      "atualizado_em": "2025-01-15T10:30:00Z"
    }
  }
}
```

### Sucesso - Listar com Paginação
```json
{
  "type": "success",
  "message": "Assinaturas listadas com sucesso",
  "data": {
    "assinaturas": [
      { ... },
      { ... }
    ],
    "paginacao": {
      "total": 45,
      "pagina": 1,
      "limite": 20,
      "total_paginas": 3
    }
  }
}
```

### Erro - Falta de Autenticação
```json
{
  "type": "error",
  "message": "Token inválido ou expirado",
  "error": "Unauthorized"
}
```

HTTP Status: 401

## Validações Implementadas

### Criar Assinatura
- [ ] aluno_id obrigatório e válido
- [ ] plano_id obrigatório e válido
- [ ] data_inicio obrigatória e no formato DATE
- [ ] Aluno não pode ter assinatura ativa para este plano
- [ ] Plano deve estar ativo
- [ ] Aluno deve estar ativo

### Renovar Assinatura
- [ ] Assinatura deve estar em status 'ativa'
- [ ] renovacoes_restantes > 0 ou ilimitado
- [ ] Data de renovação não pode ser anterior à hoje

### Suspender Assinatura
- [ ] Assinatura deve estar em status 'ativa'
- [ ] Motivo deve ser fornecido

### Reativar Assinatura
- [ ] Assinatura deve estar em status 'suspensa'
- [ ] Data de reativação não pode ser anterior à data de suspensão

### Cancelar Assinatura
- [ ] Motivo deve ser fornecido
- [ ] Assinatura não pode estar já cancelada

## Performance & Índices

```sql
-- Índices para melhor performance
CREATE INDEX idx_status ON assinaturas(status);
CREATE INDEX idx_data_vencimento ON assinaturas(data_vencimento);
CREATE INDEX idx_aluno_id ON assinaturas(aluno_id);
CREATE INDEX idx_academia_id ON assinaturas(academia_id);
CREATE INDEX idx_plano_id ON assinaturas(plano_id);

-- Query típica utiliza estes índices:
-- WHERE academia_id = ? AND status = ? AND data_vencimento > ?
```

## Segurança

1. **Autenticação JWT**
   - Token obrigatório em todos os endpoints
   - Validação de expiração

2. **Autorização**
   - Admin: acessa apenas sua academia
   - SuperAdmin: acessa todas as academias

3. **Validação de Entrada**
   - Prepared Statements (proteção contra SQL Injection)
   - Validação de tipos de dados
   - Validação de ranges

4. **Isolamento de Dados**
   - TenantMiddleware garante que admin não vê dados de outras academias
   - SuperAdmin pode ver tudo mas com filtros de auditoria

## Escalabilidade

### Para 100.000+ assinaturas:

1. **Particionamento de Tabelas**
   ```sql
   PARTITION BY RANGE(YEAR(criado_em))
   ```

2. **Caching**
   - Redis para assinaturas próximas de vencer
   - Cache de relatórios

3. **Paginação**
   - Obrigatória em listagens
   - Limite máximo de 100 registros por página

4. **Background Jobs**
   - Queue para renovações automáticas
   - Notificações assíncronas

---

**Diagrama criado em:** Fevereiro 2026  
**Versão:** 1.0.0  
**Status:** ✅ Documentação Completa
