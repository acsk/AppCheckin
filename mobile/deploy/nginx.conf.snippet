# Nginx — conteúdo de mobile/dist/ na raiz do site
#
#   location /_expo/ {
#       try_files $uri =404;
#   }
#   location = /index.html {
#       add_header Cache-Control "no-cache, no-store, must-revalidate";
#   }
#   location / {
#       try_files $uri $uri/ /index.html;
#   }
