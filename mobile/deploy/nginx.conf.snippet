# Nginx — mobile.appcheckin.com.br (root = conteúdo de dist/)
#
#   root /var/www/mobile/public_html;
#
#   location /expo-static/ {
#       try_files $uri =404;
#   }
#   location /fonts/ {
#       try_files $uri =404;
#   }
#   location = /index.html {
#       add_header Cache-Control "no-cache, no-store, must-revalidate";
#   }
#   location / {
#       try_files $uri $uri/ /index.html;
#   }
